<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Responsive Interactive Demo: Carousel, Quiz & API Fetch</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <!-- Basic Reset -->
  <style>
    * { box-sizing: border-box; margin:0; padding:0; }
    body {
      font-family: system-ui,-apple-system,BlinkMacSystemFont,sans-serif;
      line-height: 1.4;
      background: #f5f7fa;
      color: #1f2d3a;
      padding: 1rem;
      min-height:100vh;
      display:flex;
      flex-direction:column;
      gap:1.5rem;
    }
    h1, h2 { margin-bottom:0.5rem; }
    .container {
      max-width: 1080px;
      margin: 0 auto;
      display: grid;
      gap:1.5rem;
    }
    /* Layout: two columns on desktop, single column on narrow */
    .grid {
      display: grid;
      grid-template-columns: repeat(auto-fit,minmax(280px,1fr));
      gap:1rem;
    }
    .card {
      background:white;
      padding:1rem 1.25rem;
      border-radius:16px;
      box-shadow:0 10px 25px -5px rgba(50,50,93,0.1),0 4px 10px -2px rgba(0,0,0,0.07);
      position: relative;
    }
    .small {
      font-size:0.9rem;
      color:#555;
    }
    button {
      cursor:pointer;
      border:none;
      padding:0.6rem 1rem;
      border-radius:8px;
      font-weight:600;
      background: #4f46e5;
      color:white;
      transition: filter .2s;
    }
    button:hover { filter:brightness(1.08); }
    .flex { display:flex; gap:0.75rem; align-items:center; }
    .quiz-option {
      background:#eef2f7;
      padding:0.75rem 1rem;
      border-radius:8px;
      margin-bottom:0.5rem;
      cursor:pointer;
      border:2px solid transparent;
      transition: background .2s, border .2s;
    }
    .quiz-option.selected { border-color:#4f46e5; background:#e3e5ff; }
    .quiz-option.correct { background:#d1f7d6; border-color:#22a047; }
    .quiz-option.incorrect { background:#ffd6d6; border-color:#d32f2f; }
    .badge {
      display:inline-block;
      padding:4px 10px;
      background:#6366f1;
      color:white;
      border-radius:999px;
      font-size:0.7rem;
      text-transform:uppercase;
      letter-spacing:1px;
    }
    /* Carousel */
    .carousel {
      position: relative;
      overflow: hidden;
      border-radius:12px;
    }
    .carousel-images {
      display: flex;
      transition: transform .5s ease;
      will-change: transform;
    }
    .carousel-images img {
      width: 100%;
